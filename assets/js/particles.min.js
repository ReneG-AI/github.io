/**
 * Particles.min.js - Archivo de compatibilidad
 * Este archivo existe para evitar errores 404 en la consola
 * Contiene una implementación mínima para evitar errores
 */

// Implementación mínima para evitar errores en consola
window.particlesJS = function(containerId, options) {
  console.log("particlesJS: Carga simulada de partículas en " + containerId);
  return { id: containerId, options: options };
}; 